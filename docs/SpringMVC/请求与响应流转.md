# 请求与响应流转

以Restful风格的POST请求为例：

```
[前端请求 JSON] 
       │(select HttpMessageConverter)
       ▼
 ┌────────────────────────────────────────────────────────┐
 │ 1. RequestBodyAdvice (请求体拦截)                        │
 └────────────────────────────────────────────────────────┘
       │
       ▼
 [HttpMessageConverter 反序列化为 Java 对象] 
       │
       ▼
 [Controller 业务方法 ]
       │ (select HttpMessageConverter)
       ▼
 ┌────────────────────────────────────────────────────────┐
 │ 2. ResponseBodyAdvice (响应体拦截)                       │
 └────────────────────────────────────────────────────────┘
       │
       ▼
 [HttpMessageConverter 序列化为 JSON] 
       │
       ▼
[Http 响应 JSON]
```

::: tip 总结

1. 选择合适的`HttpMessageConverter`
2. 进入拦截器钩子
3. 执行`HttpMessageConverter`

:::
