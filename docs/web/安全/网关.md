# 网关

> 网络中间件（Nginx、网关、代理服务器）

在前端和后端之间，往往还隔着很多层“中间人”（比如 Nginx 负载均衡、API 网关、CDN 等）。它们也会看请求头：

- **Nginx / 网关**：会看 `Host` 头或自定义的路由头，用来决定把这个请求转发给后端的哪台 Java 服务器。
- **缓存服务器**：会看 `Cache-Control` 或者是携带缓存标签的请求头（如 `If-None-Match`），来决定是直接返回缓存，还是去问后端的 Java 服务器。
